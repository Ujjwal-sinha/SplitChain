{
  "name": "splitchain-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for SplitChain decentralized expense sharing",
  "scripts": {
    "compile": "forge build",
    "test": "forge test",
    "deploy": "forge script script/DeploySplitChain.s.sol --rpc-url https://rpc.primordial.bdagscan.com --broadcast --verify --etherscan-api-key YOUR_BLOCKDAG_EXPLORER_API_KEY_HERE -vvvv"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
